pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scipy>=1.10
scikit-learn>=1.2
opentsne>=1.0
biopython>=1.81

# --- R (not installed by pip) ---
# Tr-lncRNA limma step: tr_limma_de.R needs Bioconductor limma + CRAN jsonlite.
# After pip install above, from repo root run:
#   Rscript install_r_dependencies.R
# Then:
#   Rscript tr_limma_de.R .
# (Or use python generate_tr_lncrna_identification.py which calls both Python z-step and R.)
