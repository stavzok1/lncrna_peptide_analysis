# Pinned versions used for manuscript analyses (Python 3.11+ recommended).
# Reproduce with: pip install -r requirements.txt

matplotlib==3.10.8
numpy==2.3.4
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.8.0
biopython==1.86
logomaker==0.8.7

# Supplement / alternate Fig 1B embedding (--embedding opentsne4)
opentsne==1.0.4

# --- R (install via install_r_dependencies.R; not on PyPI) ---
# limma==3.66.0      # Bioconductor — DE + BH-FDR on adj.P.Val (tr_limma_de.R)
# jsonlite==2.0.0    # CRAN — limma_summary.json
